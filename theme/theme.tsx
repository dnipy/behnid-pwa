import { createTheme } from "@mui/material"


const theme = createTheme({
    palette:{
        primary : {
            main : '#fe6a00'
        },
        secondary : {
            main : '#0a042b'
        },
    }
})

export default theme